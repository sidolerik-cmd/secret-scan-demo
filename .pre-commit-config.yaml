repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Gitleaks (Crypto rules)
        entry: gitleaks protect --staged --config .gitleaks-crypto.toml --verbose
        language: system
        pass_filenames: false
